# ─────────────────────────────────────────────────────────────────
#  Bombino CRM  ·  Environment Variables
#  Copy this file to .env.local for local development
# ─────────────────────────────────────────────────────────────────

# ── Turso / libSQL Database ───────────────────────────────────────
# Local dev  → file-based SQLite (no auth token needed)
# Production → your Turso database URL + auth token

# Local dev:
TURSO_DATABASE_URL=file:./data/bombino.db

# Production (get these from https://app.turso.tech):
# TURSO_DATABASE_URL=libsql://your-db-name-your-org.turso.io
# TURSO_AUTH_TOKEN=your-turso-auth-token

# ── JWT Secret ────────────────────────────────────────────────────
# Used to sign session cookies. Change this to a long random string.
# Generate one: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"

JWT_SECRET=bombino-crm-jwt-secret-2024-internal

# ── Mailgun (for report emails) ───────────────────────────────────
# Sign up at https://mailgun.com — free tier allows 100 emails/day
#
# MAILGUN_API_KEY=key-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# MAILGUN_DOMAIN=mg.yourdomain.com
# MAILGUN_FROM=Bombino CRM <noreply@mg.yourdomain.com>
#
# If your Mailgun account is in the EU region, set:
# MAILGUN_URL=https://api.eu.mailgun.net
